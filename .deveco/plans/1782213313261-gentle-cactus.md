---
task: Apply glassmorphism to TabBar and EmptyState
status: in_progress
---

## Changes

### 1. MainTabsPage.ets (active tab bar)
- Add theme imports: `ThemeReactive, themeReactive, ThemeColors, ThemeMode`
- Add `themeRef` + `isDark` getter (pattern from GradientHeader.ets)
- Apply on `Tabs`: `.barOverlap(true)`, `.barBackgroundBlurStyle(BlurStyle.Thin, { adaptiveColor: AdaptiveColor.DEFAULT, scale: 1.0 })`, `.barBackgroundColor(this.isDark ? '#D9121212' : '#E6FFFFFF')`

### 2. ImmersiveTabBar.ets (standalone tab bar component)
- Add theme imports + `themeRef` + `isDark` getter
- Replace `.backdropBlur(this.blurRadius)` → `.backgroundBlurStyle(BlurStyle.Thin, { adaptiveColor: AdaptiveColor.DEFAULT, scale: 1.0 })`
- Replace rgba bg → `.backgroundColor(this.isDark ? '#D9121212' : '#E6FFFFFF')`
- Add `.border({ width: 0.5, color: this.isDark ? '#1FFFFFFF' : '#15000000' })`

### 3. EmptyState.ets
- Add theme imports + `themeRef` + `isDark` getter
- Wrap emoji in gradient circle (100x100, borderRadius 50, linearGradient)
- Title color: theme-aware primary green
- Add `.transition(TransitionEffect.OPACITY.animation({ duration: 300 }))` on root

## Build verification
- Run `build_project` with module `entry@default`
