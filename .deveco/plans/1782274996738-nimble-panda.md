---
title: Fix 5 ArkTS issues in family_food_app
---

## Task 1: RecipeCreatePage image upload (P2.5)
- Add imports: `picker, fileIo` from `@kit.CoreFileKit`, `CommunityImageService, ImageUploadResult` from `feature_community`, `common` from `@kit.AbilityKit`, `hilog` from `@kit.PerformanceAnalysisKit`
- Add `@Local coverImageUrl: string = ''`, `@Local isUploadingImage: boolean = false`
- Add cover image picker section in `formCard()` builder
- Add `pickCoverImage()` method using PhotoViewPicker → CommunityImageService.uploadImage()
- Update `handlePublish()` to pass `this.coverImageUrl`

## Task 2: Recipe→ShoppingList generation (P2.6)
- Add imports: `ShoppingItem` from `hsp_core`, `ShoppingListRepository` from `../repository/ShoppingListRepository`, `relationalStore` from `@kit.ArkData`, `common` from `@kit.AbilityKit`
- Add `repo: ShoppingListRepository` field, init in `aboutToAppear`
- Add "生成购物清单" button in `ingredientsColumn()` builder
- Add `generateShoppingList()` method creating ShoppingItem per ingredient

## Task 3: First launch allergen wizard trigger (P2.8)
- Modify `PrivacyDialogPage.navigateToHome()` to check `hasCompletedSetup` preference flag
- If not complete, navigate to `pages/AllergenSetupWizard` instead of `pages/MainTabsPage`

## Task 4: Fix NutritionTargetGauge data (P2.9)
- Add `@Trace todaySodium`, `todaySugar`, `todayFat`, `todayProtein` to DashboardViewModel
- Populate from daily budget in `refreshDashboard()`
- Update NutritionTargetGauge call to use these values

## Task 5: Fix RiskAssessmentPanel (P2.10)
- Build a CrossRiskResult from recommendations data (CAUTION/AVOID → RiskFactor)
- Pass computed riskResult instead of null
- Import CrossRiskResult, RiskFactor from feature_report
